{% extends 'base.html.twig' %}

{% block body %}
    <div id="contact" style="position: relative;top: -7rem;"></div>
    <section class="hero is-bold is-normal" style="background: center / cover no-repeat fixed url('{{ asset('build/images/email-3249062_1280.2d3dbb61.png') }}');position: relative;">
        <div class="section-bg-layer"></div>
        <div class="hero-body">
            <div class="container">
                <h2 class="subtitle is-1 has-text-light has-text-weight-bold has-text-centered-touch is-flex-desktop subtitle-left" style="align-items: baseline">
                    Contact
                    <span class="icon has-text-light is-hidden-touch" style="margin: 0 1.75rem">
                    <i class="fas fa-arrow-alt-circle-left"></i>
                </span>
                </h2>
            </div>
        </div>

        {{ form_start(messageForm) }}
        {% include 'sections/contact/includes/contact_form.html.twig' %}
        {{ form_end(messageForm) }}

    </section>
{% endblock %}
