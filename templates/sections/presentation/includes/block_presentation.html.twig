

<div class="is-centered block-presentation" style="position:relative;{% if presentation.imgName %}background: center / cover no-repeat url('{{ vich_uploader_asset(presentation, 'img') | imagine_filter('HQ') }}');{% endif %}">

    <div style="position:absolute;width: 100%;height: 100%;top: 0;left: 0;background: rgba(8,17,8,0.8)"></div>

        <div class="container column is-two-thirds is-full-touch">

            <div class="column mb-4" style="border-radius: 3px;background: rgba(18,18,18,0.9)">

                {% if presentation.title %}
                <p class="title is-1 is-spaced has-text-primary has-text-centered is-uppercase">{{ presentation.title }}</p>
                {% endif %}

                {% if presentation.subTitle %}
                <p class="subtitle is-3 has-text-primary has-text-centered">{{ presentation.subTitle }}</p>
                {% endif %}

            </div>

            <article class="message is-primary text-presentation" style="background: none">
                <div class="message-body has-text-light" style="border-top-right-radius: 0px;border-bottom-right-radius: 0px;background: rgba(18,18,18,0.9)">
                    {{ presentation.text }}
                    {% if presentation.buttonLink %}
                        <div class="has-text-centered column">
                            <a href="{{ presentation.buttonLink }}" class="button is-green-custom has-text-weight-bold" style="border-radius: 0px">En savoir plus...</a>
                        </div>

                    {% endif %}
                    {% if is_granted('ROLE_ADMIN') %}
                        <hr class="has-background-white">
                        <div style="display: flex;">
                            <div class="column half-touch has-text-centered">
                                <span class="icon is-medium">
                                    <a style="text-decoration: none" href="{{ path('del_presentation', {'id': presentation.id}) }}" class="fas fa-2x fa-times-circle has-text-danger" title="supprimer la présentation"></a>
                                </span>
                            </div>

                            <div class="column half-touch has-text-centered">
                                <span class="icon is-medium">
                                    <a style="text-decoration: none" href="{{ path('edit_presentation', {'id': presentation.id}) }}" class="fas fa-2x fa-edit has-text-warning" title="éditer la présentation"></a>
                                </span>
                            </div>
                        </div>
                    {% endif %}
                </div>

            </article>



        </div>

</div>



